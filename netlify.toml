[build]
  command   = "pnpm --filter @workspace/nativos-3d run build"
  publish   = "artifacts/nativos-3d/dist/public"

[build.environment]
  NODE_ENV  = "production"
  BASE_PATH = "/"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
